services:
  webhook:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    environment:
      PORT: 5000
      SESSION_DB_PATH: /data/sessions.db
    volumes:
      - sessions:/data

volumes:
  sessions:
